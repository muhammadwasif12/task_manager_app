class AppDateUtils {
  // Utility method to format DateTime as 'yyyy-MM-dd'
  static String formatDate(DateTime date) {
    return '${date.year}-${date.month.toString().padLeft(2, '0')}-${date.day.toString().padLeft(2, '0')}';
  }
}
